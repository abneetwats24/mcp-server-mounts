services:
  mcp-server:
    build: .
    container_name: mcp-server
    ports:
      - "3000:3000"
    volumes:
      - venv_data:/app/.venv
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped

volumes:
  venv_data:
