MCP_HOST=127.0.0.1
MCP_PORT=3000

OAUTH_CLIENT_ID=mcp-tools-client
OAUTH_CLIENT_SECRET=replace-with-real-secret

# Optional: override default scope used in AuthSettings.required_scopes
MCP_REQUIRED_SCOPE=mcp:tools

# Optional: override default issuer URL if not using the baked-in value
OAUTH_ISSUER_URL=http://192.168.10.7:5555/realms/openspace
